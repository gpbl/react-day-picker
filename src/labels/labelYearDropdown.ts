import type { FormatOptions } from 'date-fns';

/** The default ARIA label for the years dropdown. */
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function labelYearDropdown(_options?: FormatOptions) {
  return 'Year: ';
}
