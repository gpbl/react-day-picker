import { UI } from "./types";

/** The class names that can passed to DayPicker's `classNames` prop. */
const classNames: {
  [uiElement in UI]: string;
// eslint-disable-next-line prettier/prettier
}

export default classNames;
