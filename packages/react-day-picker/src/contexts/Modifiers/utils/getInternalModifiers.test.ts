test.todo('should transform to array the modifiers from the "selected" prop');
test.todo('should transform to array the modifiers from the "disabled" prop');
test.todo('should transform to array the modifiers from the "hidden" prop');
test.todo('should transform to array the today prop');

describe('when navigation is limited "from date"', () => {
  test.todo('should push the before matcher to the "disabled" modifiers');
});

describe('when navigation is limited "to date"', () => {
  test.todo('should push the after matcher to the "disabled" modifiers');
});

describe('when in multiple select mode', () => {
  test.todo(
    'should add the Disabled modifiers from the select multiple context'
  );
});

describe('when in range select mode', () => {
  test.todo('should add the Disabled modifiers from the SelectRange context');
  test.todo('should set the RangeStart modifier from the SelectRange context');
  test.todo('should set the RangeMiddle modifier from the SelectRange context');
  test.todo('should set the RangeEnd modifier from the SelectRange context');
});
