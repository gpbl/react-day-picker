test.todo('should return the default caption layout');

describe('when the caption layout is not "buttons"', () => {
  describe('when fromDate is undefined', () => {
    test.todo('should return "buttons" as caption layout');
  });
  describe('when toDate is undefined', () => {
    test.todo('should return "buttons" as caption layout');
  });
});

describe('when fromMonth', () => {
  test.todo('fromDate should be the start of month');
  describe('when also fromYear', () => {
    test.todo('fromDate should be the start of month');
  });
});

describe('when fromYear', () => {
  test.todo('fromDate should be the start of year');
});

describe('when toMonth', () => {
  test.todo('toDate should be the start of month');
  describe('when also toYear', () => {
    test.todo('toDate should be the start of month');
  });
});

describe('when toYear', () => {
  test.todo('toDate should be the start of year');
});
