import { NavButtonLabelFormatter } from '../../../types';

/**
 * The default ARIA label for next month button in navigation
 */
export const labelNext: NavButtonLabelFormatter = (): string => {
  return 'Go to next month';
};
