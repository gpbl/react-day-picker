describe('when the day to render is hidden', () => {
  test.todo('should render an empty fragment');
});
describe('when the day to render is not a `button`', () => {
  test.todo('should render a `div`');
});
describe('when the day to render is a `button`', () => {
  test.todo('should render a `button`');
});
