import { MatchingModifiers } from '../types';

/**
 * Default modifiers.
 */
export const defaultModifiers: MatchingModifiers = {
  disabled: false,
  hidden: false,
  interactive: true,
  aftermonth: false,
  beforemonth: false,
  selected: false,
  today: false
};
