// TODO: move to enum
/** Represent the modifiers that are used internally in DayPicker. */
export type InternalModifier = 'selected' | 'disabled' | 'hidden';
// | 'range_start'
// | 'range_end'
// | 'range_middle';
