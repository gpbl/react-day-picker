---
title: Ethiopic Calendar
---

DayPicker supports the [Ethiopic calendar](https://en.wikipedia.org/wiki/Ethiopian_calendar) via a dedicated build.

Install the core package and the Ethiopic calendar package:

```bash npm2yarn
npm install react-day-picker@next @daypicker/ethiopic@next
```

Import `DayPicker` from `@daypicker/ethiopic` to display Ethiopic months, weekdays, and numerals while keeping the usual DayPicker API.

```tsx
import { DayPicker } from "@daypicker/ethiopic";
```

### Basic usage

By default, the locale is `amET` with Ge'ez numerals. The `amET` locale is
owned and exported by `@daypicker/ethiopic`.

```tsx
import { DayPicker, amET } from "@daypicker/ethiopic";

<DayPicker locale={amET} />;
```

<BrowserWindow sourceUrl="https://github.com/gpbl/react-day-picker/blob/release/v10-next/examples/Ethiopic.tsx">
  <Examples.Ethiopic />
</BrowserWindow>

### Latin numerals

Switch to Latin numerals:

```tsx
import { DayPicker } from "@daypicker/ethiopic";

<DayPicker numerals="latn" />;
```

<BrowserWindow sourceUrl="https://github.com/gpbl/react-day-picker/blob/release/v10-next/examples/EthiopicLatin.tsx">
  <Examples.EthiopicLatin />
</BrowserWindow>
