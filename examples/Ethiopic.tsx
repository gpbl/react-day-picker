import React from "react";

import { DayPicker } from "react-day-picker/ethiopic";

import type { DayPickerProps } from "react-day-picker";

export function Ethiopic(props: DayPickerProps) {
  return <DayPicker {...props} />;
}
