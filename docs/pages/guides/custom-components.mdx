import {
  SourceCode,
  getSourceCodeStaticProps
} from '../../components/SourceCode';
import { RenderingBox } from '../../components/RenderingBox';

import CustomCaption from '@examples/custom-caption.tsx';
import CustomDay from '@examples/custom-day.tsx';
import RangeShiftKey from '@examples/range-shift-key.tsx';
import CustomDisableRow from '@examples/custom-disable-row.tsx';

import 'react-day-picker/dist/style.css';

export const getStaticProps = () =>
  getSourceCodeStaticProps([
    'custom-caption.tsx',
    'custom-day.tsx',
    'range-shift-key.tsx',
    'custom-disable-row.tsx'
  ]);

# Custom Components

Use the `components` prop to replace some of the internal components used by DayPicker with a custom implementation.

import { Callout } from 'nextra/components';

<Callout emoji="🚧">
  This section is still under construction. If you need help, please reach out
  the maintainers in the [discussion
  page](https://github.com/gpbl/react-day-picker/discussions) on GitHub.
</Callout>

## DayPicker hooks

When creating custom components, you will find useful the DayPicker hooks.

## Examples

### Custom Caption

Implement a custom `Caption` component with next/previous buttons. Note the use of the [useNavigation hook](/api/functions/useNavigation) to navigate between months.

<SourceCode source="custom-caption.tsx" />

<RenderingBox>
  <CustomCaption />
</RenderingBox>

### Wrapping the day

<SourceCode source="custom-day.tsx" />

<RenderingBox>
  <CustomDay />
</RenderingBox>

### Range selections with Shift key

Implement a custom `Day` component to select ranges while pressing the <kbd>Shift</kbd> key.

<SourceCode source="range-shift-key.tsx" />

<RenderingBox>
  <RangeShiftKey />
</RenderingBox>

### Disable rows in the past

Implement a custom component to hide the rows of past weeks.

<SourceCode source="custom-disable-row.tsx" />

<RenderingBox>
  <CustomDisableRow />
</RenderingBox>
