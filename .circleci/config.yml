version: 2.0
jobs:
  build:
    machine:
      enabled: true
    steps:
      - type: checkout
      - type: shell
        name: Say Hello
        command: |
          echo "Hello There"
  test:
    steps:
      - type: shell
        name: Say Goodbye
        command: |
          echo "Hello Goodbye"

workflows:
  version: 2.0
  build-and-test:
    - build
    - test:
        requires:
          - build