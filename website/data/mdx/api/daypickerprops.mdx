---
section: Interface
title: DayPickerProps
---

<SectionTitle>Interface</SectionTitle>

# DayPickerProps

import { apiDefs } from '@/data/apiDefs';

<Description>{apiDefs.DayPickerProps.shortComment}</Description>

---

<RenderJsx jsx={apiDefs.DayPickerProps.typeDefJsx} />

## PropsBase

### Styling Props

<PropsList member="PropsBase" topic="styling" />

### Single Mode

These props are available only when `mode` is set to `single`.

<PropsList member="PropsSingle" />

### Multi Mode

These props are available only when `mode` is set to `multi`.

<PropsList member="PropsMulti" />

### Range Mode

These props are available only when `mode` is set to `range`.

<PropsList member="PropsRange" />
