---
section: API Reference
title: DayPickerProps
---

<SectionTitle>API Reference</SectionTitle>

# DayPickerProps

<Description>
  <ApiDescription member="DayPickerProps" />
</Description>
---

<PropsTable member="PropsBase" />

### Single Mode

These props are available only when `mode` is set to `single`.

<PropsTable member="PropsSingle" />

### Multi Mode

These props are available only when `mode` is set to `multi`.

<PropsTable member="PropsMulti" />

### Range Mode

These props are available only when `mode` is set to `range`.

<PropsTable member="PropsRange" />
