---
section: Customization
title: Calendar Options
description: Change how months, weeks and days are displayed in the calendar.
---

<SectionTitle>Customization</SectionTitle>

# Calendar Options

<Description>
  Change how months, weeks and days are displayed in the calendar..
</Description>

---

## Calendar Options Props

<PropsTable
  data={[
    {
      name: 'showOutsideDays',
      required: false,
      type: 'boolean',
      description: 'Display the days from the previous and next months.',
      default: 'false'
    },
    {
      name: 'numberOfMonths',
      required: false,
      type: 'number',
      description: 'The number of months to display.',
      default: '1'
    },
    {
      name: 'pagedNavigation',
      required: false,
      type: 'boolean',
      description: 'Navigate the months using the navigation buttons.',
      default: 'false'
    },
    {
      name: 'fixedWeeks',
      required: false,
      type: 'boolean',
      description: 'Display six weeks per month.',
      default: 'false'
    },
    {
      name: 'showWeekNumber',
      required: false,
      type: 'boolean',
      description: 'Display the week numbers.',
      default: 'false'
    },
    {
      name: 'weekStartsOn',
      required: false,
      type: 'number',
      description: 'Set the first day of the week.',
      default: '0'
    },
    {
      name: 'firstWeekContainsDate',
      required: false,
      type: 'number',
      description: 'Set the day of January in the first week of the year.',
      default: '1'
    },
    {
      name: 'formatters.formatWeekNumber',
      required: false,
      type: '(weekNumber: number) => string',
      description: 'Change how week numbers are displayed.',
      default: '(weekNumber) => weekNumber'
    }

]}
/>

## Display the Outside Days

The outside days are the days from the previous and next months displayed in the calendar. Use `showOutsideDays` to display them.

<PreviewBox>
  <Examples.OutsideDays />
</PreviewBox>

```tsx
<DayPicker showOutsideDays />
```

## Render Multiple Months

Use `numberOfMonths` to render more than one month in the calendar.

<PreviewBox>
  <Examples.MultipleMonths />
</PreviewBox>

```tsx
<DayPicker numberOfMonths={2} />
```

### Paged Navigation

Toggle `pagedNavigation` to navigate the number of months per time.

<PreviewBox>
  <Examples.MultipleMonthsPaged />
</PreviewBox>

```tsx
<DayPicker numberOfMonths={2} pagedNavigation />
```

## Using Fixed Weeks

Use `fixedWeeks` to display six weeks per months. This will prevent the calendar
to change its height when navigating.

<PreviewBox>
  <Examples.Fixedweeks />
</PreviewBox>

```tsx
<DayPicker fixedWeeks />
```

## Show the Week Numbers

Use `showWeeknumber` to display the week numbers. When this prop is set, use
`onWeeknumberClick` to handle the click on a week number.

<PreviewBox>
  <Examples.Weeknumber />
</PreviewBox>

```tsx
<DayPicker
  showWeekNumber
  onWeekNumberClick={(weekNumber) => {
    console.log(weekNumber);
  }}
/>
```

### Customize the Week Numbers

- To set the first day of the week, use `weekStartsOn`.
- To set the day of January in the first week of the year
  `firstWeekContainsDate`.
- Use the `formatWeeknumber` formatter to change how week numbers are displayed.

<PreviewBox>
  <Examples.Weeknumber />
</PreviewBox>

```tsx
<DayPicker
  weekStartsOn={6} // Saturday as first day of the week
  firstWeekContainsDate={1} // Number the first week of the year from day 1
  showWeekNumber
  labels={{
    labelWeekNumber: (weekNumber: number) => `Week ${weekNumber}`
  }}
  formatters={{
    // Add `W` prefix to week number
    formatWeekNumber: (weekNumber: number) => `W${weekNumber}`
  }}
/>
```
