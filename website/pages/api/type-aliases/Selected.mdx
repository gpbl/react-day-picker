<SectionTitle>TypeAlias (Selection Modes)</SectionTitle>

# Selected\<T\>

<Description>
The selected value when in selection mode.
</Description>

---

<a id="undefined" name="undefined"></a>

<SignatureMemberIdentifier>**Selected**\<`T`\>: `T` extends `"single"` ? `Date` : `T` extends `"multi"` ? `Date`[] : `T` extends `"range"` ? [`DateRange`](/api/type-aliases/DateRange.md) : `undefined`</SignatureMemberIdentifier>

The selected value when in selection mode.

## Type parameters

| Type parameter |
| :------ |
| `T` extends [`Mode`](/api/type-aliases/Mode.md) |

## Source

[Users/gp/Developer/react-day-picker/v9/src/types/props.ts:417](https://github.com/gpbl/react-day-picker/blob/b97c86295/src/types/props.ts#L417)
