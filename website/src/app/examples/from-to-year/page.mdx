import { FromToYear } from 'react-day-picker/examples/FromToYear';

# FromToYear



<FromToYear />

```tsx
export function FromToYear() {
  return (
    <DayPicker defaultMonth={new Date(2024, 0)} fromYear={2024} toYear={2026} />
  );
}
```
