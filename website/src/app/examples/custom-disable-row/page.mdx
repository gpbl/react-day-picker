import { CustomDisableRow } from 'react-day-picker/examples/CustomDisableRow';

# CustomDisableRow



<CustomDisableRow />

```tsx
export function CustomDisableRow() {
  return (
    <DayPicker
      fromDate={new Date()}
      components={{ WeekRow: OnlyFutureRow }}
      hidden={isPastDate}
      showOutsideDays
      today={new Date(2021, 10, 25)}
    />
  );
}
```
