import { MultipleMinMax } from 'react-day-picker/examples/MultipleMinMax';

# MultipleMinMax



<MultipleMinMax />

```tsx
export function MultipleMinMax() {
  const [selected, setSelected] = useState<Date[]>();

  return (
    <DayPicker
      mode="multi"
      min={2}
      max={5}
      selected={selected}
      onSelect={setSelected}
    />
  );
}
```
