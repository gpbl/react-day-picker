import { ModifiersClassnames } from 'react-day-picker/examples/ModifiersClassnames';

# ModifiersClassnames



<ModifiersClassnames />

```tsx
export function ModifiersClassnames() {
  return (
    <>
      <style>{style}</style>
      <DayPicker
        defaultMonth={bookedDays[0]}
        modifiers={{
          booked: bookedDays
        }}
        modifiersClassNames={{
          booked: 'my-booked-class'
        }}
      />
    </>
  );
}
```
