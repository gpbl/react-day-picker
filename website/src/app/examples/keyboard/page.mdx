import { Keyboard } from 'react-day-picker/examples/Keyboard';

# Keyboard



<Keyboard />

```tsx
export function Keyboard(props: DayPickerProps) {
  return <DayPicker {...props} today={new Date(2022, 5, 10)} />;
}
```
