import { version } from 'react-day-picker/package.json';

export const dependencies = {
  '@babel/runtime': 'latest',
  'react-day-picker': version,
  'date-fns': 'latest',
  'react-dom': 'latest',
  react: 'latest'
};
