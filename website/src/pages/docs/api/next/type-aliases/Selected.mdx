# Selected\<T\>

<a id="undefined" name="undefined"></a>

> **Selected**\<`T`\>: `T` extends `"single"` ? `Date` : `T` extends `"multi"` ? `Date`[] : `T` extends `"range"` ? [`DateRange`](/docs/api/next/type-aliases/DateRange.md) : `undefined`

The selected value when in selection mode.

## Type parameters

| Type parameter |
| :------ |
| `T` extends [`Mode`](/docs/api/next/type-aliases/Mode.md) |

## Source

[src/types/props.ts:415](https://github.com/gpbl/react-day-picker/blob/next/src/types/props.ts#L415)
