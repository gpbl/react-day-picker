import React from 'react';

/**
 * Render the documentation for the {{name}} hook.
 */
export function {{name}}(props) {
  const reflection = props.reflection;
  return (
    <div>
      <h2>{reflection.name}</h2>
    </div>
  );
}
