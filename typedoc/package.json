{
  "name": "typedoc",
  "version": "1.0.0",
  "scripts": {
    "build": "pnpm build:main && pnpm build:next",
    "build-watch": "pnpm build:next --watch",
    "build:main": "typedoc --options typedoc.latest.mjs",
    "build:next": "typedoc --options typedoc.next.mjs"
  },
  "dependencies": {
    "react-day-picker": "workspace:*",
    "react-day-picker-next": "workspace:*",
    "typedoc": "^0.25.9",
    "typedoc-plugin-frontmatter": "0.0.3-next.0",
    "typedoc-plugin-markdown": "4.0.0-next.53"
  }
}
