# shadcn-app-workspace

This is a standalone shadcn-style Vite app wired to the workspace
`react-day-picker` package.

It keeps the generated shadcn `components/ui/calendar.tsx` shape as close as
possible to the docs so you can compare its rendering directly against the
released app in a separate browser tab.
